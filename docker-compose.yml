version: '3.8'

services:
  api:
    build: .
    container_name: poc-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - TZ=America/Sao_Paulo
    env_file:
      - .env